project_id = "dtpotzigctinidoxgooo"

[api]
enabled = true

[db]
port = 54322
major_version = 15

[functions.shift-reminder]
verify_jwt = false

[functions.shift-reminder.schedule]
# Poll every minute. Actual send time is read from app_settings.shift_reminder_time
# (Africa/Cairo wall clock). History dedupe prevents duplicate deliveries.
cron = "* * * * *"
