project_id = "dtpotzigctinidoxgooo"

[api]
enabled = true

[db]
port = 54322
major_version = 15

[functions.shift-reminder]
verify_jwt = false

# Schedule is managed by pg_cron (see migrations), not config.toml.
# Older CLI rejected [functions.shift-reminder.schedule].
# cron = "* * * * *"
